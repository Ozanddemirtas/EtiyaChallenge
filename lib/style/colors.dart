import 'package:flutter/material.dart';

class ThemeCol {
  static Color primaryColor = Color.fromARGB(255, 241, 28, 13);
  static const Color bgColor = Color.fromARGB(255, 233, 227, 227);
  static const Color titleColor = Colors.grey;
  static const Color textColor = Colors.black;

  static var green = Colors.green[700] ?? Colors.green;
  static var red = Colors.redAccent[700] ?? Colors.red;
}
